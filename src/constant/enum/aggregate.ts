enum Aggregate {
  Bootcamp = '$bootcamp',
  User = '$user',
  Course = '$course',
  Feedback = '$feedback',
  Rating = '$rating',
  AverageRating = '$averageRating',
  Tuition = '$tuition',
}

export default Aggregate
